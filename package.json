{
  "name": "leave-app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-deps": "npm install && cd server && npm install && cd ../client && npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["react", "express", "postgresql", "authentication", "leave-management"],
  "author": "",
  "license": "ISC",
  "description": "Full-stack leave management application with React frontend and Express backend",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}